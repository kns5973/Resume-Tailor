# Runtime dependencies for Resume Tailor.
#
# This mirrors the union of the [project] dependencies + the `phase1` and
# `web` extras in pyproject.toml. Keep it in sync with pyproject.toml.
#
# Note: PDF rendering needs the `pdflatex` system binary (TeX Live) and cert
# OCR needs `tesseract-ocr` — install those on the host, not here.

# --- base ---
pydantic>=2.7
jinja2>=3.1
requests>=2.31


# --- phase1 (ingestion + retrieval; pulls torch via sentence-transformers) ---
pygithub>=2.3
pdfplumber>=0.11
trafilatura>=1.10
pytesseract>=0.3.10
chromadb>=1.0
sentence-transformers>=3.0

# --- web (FastAPI frontend) ---
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
python-multipart>=0.0.9
